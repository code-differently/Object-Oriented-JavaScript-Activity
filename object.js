 var post = newPost();
    newPost.name = "Kym";
    newPost.message = "Learning to code is learning a new language!";
    newPost.numWords = function(numWords) {
        if (message.length < 141) {
            return post;
        } else {
            return("More than 140 characters!")
        }
    } 

    class post {
        constructor(name, message) {
          this.name = name;
          this.message = "View message", "Add letter", "Remove letter";
        }
      }